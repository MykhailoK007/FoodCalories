import React from 'react';
import TopBar from './components/TopBar/TopBar';

const MenuSchedule = () => {
  const sta = '';
  return (
    <div>
      {sta}
      <TopBar />
    </div>
  );
};

export default MenuSchedule;
